  <%= form_for [@author,@translate_author],  :html => { :class => "book" } do |f| %>
  <div class="form-group">
    <%= label_tag :name, t(:author) %> 
    <%= label_tag :name, @author.name , :class => 'form-control'%>
  </div>
  <div class="form-group">
    <%= f.text_field :name, :class => 'form-control kan-ime' %>
  </div>
  <div class="form-group">
    <%= link_to(clean_link(@author.books.first.book_description.link), target: '_blank') do %> <%= fa_icon "book 2x" %> <% end %>
  </div>
  <%= f.submit t(:translate), class: "btn btn-default"%>
  <% end %>