<div class="row"> 
  <div class="col-lg-5">
    <%= form_for [@book,@translate],  :html => { :class => "book" } do |f| %>
    <div class="form-group">
      <%= f.text_field :book_title, :class => 'form-control kan-ime' %>
    </div>
    <div class="form-group">
      <%= text_field_tag :author, @author.name, :class => 'form-control kan-ime' %>
    </div>
    <div class="form-group">
      <%= text_field_tag :publisher, @publisher.name,  :class => 'form-control kan-ime' %>
    </div>
    <div class="form-group">
      <%= link_to(clean_link(@book.book_description.link), target: '_blank') do %> <%= fa_icon "book 2x" %> <% end %>
      <%= f.submit t(:translate), class: "btn btn-default"%> 
    </div>
    <% end %>
  </div>
  <div class="col-lg-4">
  </div>
</div>